# Finding Best Peptides and Natural Molecules for Human GHS-R Using Boltz-2 and RAPiDock

This repository provides a practical screening workflow for **human GHS-R (GHSR1a, UniProt Q92847)**.

- **Small molecules** are scored with Boltz-2 affinity head.
- **Peptides** are docked with RAPiDock, then converted to approximate IC50 values.

The final table is unified in `results/ranking.csv`.

## What this pipeline does

- Builds Boltz YAML inputs for small molecules from `data/ligand_library.csv`.
- Runs Boltz-2 batch prediction for small molecules.
- Builds RAPiDock batch input CSV for peptides.
- Runs RAPiDock peptide docking/scoring separately.
- Parses both tracks into one ranking table plus plots and top-pose exports.

## Important modeling caveats

- Boltz-2 affinity head is used only for **small molecules** in this repo.
- Peptide IC50 values are **derived** from RAPiDock REF2015 scores:
  - `dG ~= REF2015` (kcal/mol),
  - `Kd = exp(dG / (R*T))`,
  - `IC50 ~= Kd` (reported in uM).
- Absolute peptide IC50 values are approximate; relative ranking is the intended use.
- Native ghrelin has an octanoylated Ser3 PTM that is not directly represented by standard sequence-only inputs and is not in RAPiDock's supported residue set.
- Treat all model rankings as triage signals, not final biological activity claims.

## Project layout

- `data/target_ghsr.fasta`: receptor sequence (Q92847).
- `data/ligand_library.csv`: curated ligands (editable).
- `data/ghsr_receptor.pdb`: extracted receptor chain from 7F9Z (generated by `prep_receptor.py`).
- `data/ghsr_pocket.pdb`: optional pocket-truncated receptor for RAPiDock.
- `scripts/prep_receptor.py`: one-time receptor prep from PDB.
- `scripts/build_inputs.py`: CSV -> Boltz YAML generator (small molecules only).
- `scripts/run_screen.py`: Boltz batch runner.
- `scripts/build_rapidock_inputs.py`: peptide CSV -> RAPiDock multi-task CSV.
- `scripts/run_rapidock.py`: RAPiDock wrapper runner.
- `scripts/parse_rapidock_results.py`: RAPiDock score parser and IC50 conversion for peptides.
- `scripts/parse_results.py`: merge Boltz + RAPiDock rows into `results/ranking.csv`.
- `scripts/visualize.py`: plots + top pose exports.
- `notebooks/boltz2_ghsr_screen.ipynb`: Colab workflow notebook (Boltz-focused).
- `notebooks/rapidock_colab_runner.ipynb`: Colab workflow notebook (RAPiDock upload/download).
- `notebooks/rapidock_colab_drive_runner.ipynb`: Colab workflow notebook (RAPiDock reads/writes Google Drive; optional py3Dmol receptor+peptide preview, title 13).

## Quickstart

1. Install dependencies for this repo:
   - `pip install -r requirements.txt`
2. Prepare receptor once:
   - `python scripts/prep_receptor.py --pdb-id 7F9Z --chain-id R --skip-pocket`
3. Run small-molecule Boltz track:
   - `python scripts/build_inputs.py`
   - `python scripts/run_screen.py --msa-mode server --recycling-steps 3 --diffusion-samples 1`
4. Run peptide RAPiDock track (in RAPiDock env):
   - `python scripts/build_rapidock_inputs.py --receptor-pdb data/ghsr_receptor.pdb`
   - `python scripts/run_rapidock.py --rapidock-dir /path/to/RAPiDock`
5. Parse and merge:
   - `python scripts/parse_rapidock_results.py`
   - `python scripts/parse_results.py`
6. Visualize:
   - `python scripts/visualize.py`

## Google Colab notes (RAPiDock)

RAPiDock includes precompiled Linux extension modules (for example `PeptideBuilder.so`) that are built against **CPython ≤3.11**. Colab’s default notebook runtime is often **Python 3.12**, where imports can fail with errors like `undefined symbol: _PyUnicode_Ready`.

Use the Colab notebooks in `notebooks/`, which install **Miniforge + a Python 3.10 conda environment** and run `inference.py` via `conda run`.

## Step-by-step: peptide IC50 from RAPiDock

1. **Prepare receptor PDB once**
   - `python scripts/prep_receptor.py --pdb-id 7F9Z --chain-id R --skip-pocket`
2. **Check peptide rows in library**
   - Ensure peptide rows have `type=peptide`.
   - Fill `sequence` and optionally `peptide_seq_rapidock` for non-canonical residues.
3. **Build RAPiDock input CSV**
   - `python scripts/build_rapidock_inputs.py --library data/ligand_library.csv --receptor-pdb data/ghsr_receptor.pdb --out-dir inputs_rapidock`
   - Output: `inputs_rapidock/protein_peptide.csv`
4. **Set up RAPiDock environment**
   - Follow RAPiDock install docs (`rapidock_env.yaml` or manual setup).
   - Ensure checkpoint exists under RAPiDock model directory.
5. **Run RAPiDock**
   - `python scripts/run_rapidock.py --rapidock-dir /path/to/RAPiDock --inputs-csv inputs_rapidock/protein_peptide.csv --outputs-dir outputs_rapidock --ckpt rapidock_local.pt --N 10 --batch-size 4 --cpu 8`
   - Expected per peptide: `outputs_rapidock/<complex_name>/ref2015_score.csv` (requires PyRosetta import to succeed in the RAPiDock env).
6. **Parse RAPiDock peptide scores into IC50 estimates**
   - `python scripts/parse_rapidock_results.py --library data/ligand_library.csv --outputs-dir outputs_rapidock --results-dir results`
   - Output: `results/ranking_peptides.csv`
7. **Merge with Boltz small-molecule ranking**
   - `python scripts/parse_results.py`
   - Output: `results/ranking.csv`
8. **Optional plots and top poses**
   - `python scripts/visualize.py`

## Editing the ligand library

`data/ligand_library.csv` columns:

- `name`: unique ligand identifier.
- `type`: `small_molecule` or `peptide`.
- `source`: provenance (`natural`, `endogenous`, `positive_control`, etc.).
- `smiles`: required for `small_molecule`.
- `sequence`: peptide sequence.
- `peptide_seq_rapidock`: optional RAPiDock residue encoding for non-canonical residues (for example `[DTR]`, `[DPN]`, `[AIB]`).
- `notes`: optional curation comments.

Rows missing required fields are skipped by the relevant build script.

## Ranking interpretation

- `affinity_pred_value_log10IC50uM`: lower is better.
  - Small molecules: Boltz-2 affinity output.
  - Peptides: RAPiDock-derived approximation from REF2015.
- `affinity_pred_IC50_uM`: `10^(affinity_pred_value_log10IC50uM)`.
- `affinity_probability_binary`: Boltz binder confidence (small molecules only).
- `source_method`:
  - `boltz2_affinity` for Boltz small molecules,
  - `rapidock_ref2015` for RAPiDock peptides.

Suggested readout:

- **Small molecules**: rank by affinity first, then inspect confidence/pose plausibility.
- **Peptides**: rank by RAPiDock-derived affinity first, then review poses and biology context.

## Expected outputs

- `results/ranking.csv`
- `results/ranking_peptides.csv`
- `results/plots/*.png`
- `results/top_poses/*.pdb`

## Notes on curation quality

The default CSV intentionally includes a mixed-confidence starter set. For production screening, fill missing SMILES/sequences with validated chemistry/sequence information before long runs.

## 3D structure visualization (receptor + ligand)

### GHS-R + small molecules (Boltz-2)

Boltz writes prediction structures under `outputs/<ligand_id>/` (typically a single PDB containing receptor and ligand). After `python scripts/visualize.py`, copies also land in `results/top_poses/*.pdb` for quick inspection.

- **Colab / Jupyter**: install `py3Dmol` and load a pose PDB; use cartoon for the protein and sticks for the ligand. Example pattern: [`notebooks/boltz2_ghsr_screen.ipynb`](notebooks/boltz2_ghsr_screen.ipynb) (cartoon + `resn: ['LIG']` for sticks). If sticks do not highlight the ligand, inspect the PDB for the actual HETATM/`resn` label and adjust the selection.

### GHS-R + peptides (RAPiDock)

RAPiDock does **not** put receptor and peptide in one ranked pose file. For each complex folder under `outputs_rapidock/<complex_name>/`:

- **Receptor (docking frame):** `<complex_name>_protein_raw.pdb` (example: `ipamorelin_protein_raw.pdb`; chain **R** in typical 7F9Z extracts).
- **Peptide pose:** `rank<N>_ref2015.pdb` (or `rank<N>.pdb` if REF2015 was skipped); chain **A** is common for the peptide.

Load **both** files in the same viewer (two objects / two `addModel` calls) to see GHS-R with the docked peptide.

- **Colab (Drive workflow):** optional **title 13** in [`notebooks/rapidock_colab_drive_runner.ipynb`](notebooks/rapidock_colab_drive_runner.ipynb) previews receptor + peptide with py3Dmol after batch outputs exist.
- **Desktop:** PyMOL or ChimeraX — open the receptor PDB, then open the peptide rank PDB as a second structure; cartoon/surface on receptor, sticks or cartoon on peptide.